# Growth Curves: 96-Well Plate Inflection-Time Analysis

This repository contains an R workflow for analyzing 96-well plate-reader growth curves and estimating the inflection time for each populated well.

The analysis fits a smoothing spline to each growth curve and identifies the inflection time based on the maximum second derivative. A plate-layout template representing the organization of the 96-well plate is required. The layout is flexible and can be adapted to different experimental designs. The code uses the sample names assigned to individual wells in the plate layout to organize the analysis and group the resulting graphs. Users are encouraged to retain a copy of the plate layout for each experimental run so that results can be traced back to the original plate organization.

The current version of the code is designed to calculate inflection time and does not automatically calculate hold time. Hold time may be derived by subtracting the inflection time of bacterial control wells from the inflection time of the corresponding experimental wells.

This workflow was developed as a tool to facilitate the analysis of high-throughput plate-reader data. Users should review the generated graphs and calculated values to confirm that the identified inflection times are consistent with the observed growth curves.

## Repository structure

``` text
Growth_curves/
├── Example/
│   ├── Example_data_1.txt
│   ├── Example_RunAnalysis.R
│   ├── Output/
│   └── plate_layout.xlsx
├── Growth_curves.Rproj
├── install_packages.R
├── R/
│   └── hold_time_analysis.R
├── README.md
└── RunAnalysis.R
```

### Files

-   `RunAnalysis.R`\
    Main script for analyzing your own data. When run, it asks for the path to the plate-reader `.txt` file and the plate-layout `.xlsx` file.

-   `R/hold_time_analysis.R`\
    Contains the functions used for plate organization, growth-curve analysis, inflection-time calculation, plotting, summary generation, and output creation. Users normally do not need to edit this file.

-   `install_packages.R`\
    Installs the R packages required by the analysis.

-   `Growth_curves.Rproj`\
    RStudio project file. Open this file to work with the repository in RStudio.

-   `Example/Example_RunAnalysis.R`\
    Runs the analysis using the included example files.

-   `Example/Example_data_1.txt`\
    Example 96-well plate-reader dataset.

-   `Example/plate_layout.xlsx`\
    Example plate layout matching the included dataset.

-   `Example/Output/`\
    Folder for output generated by the example analysis.

## Requirements

R and the following packages are required:

-   lubridate
-   ggplot2
-   dplyr
-   tidyr
-   tibble
-   ggprism
-   plotly
-   htmltools
-   openxlsx
-   knitr

Install the required packages once by running:

``` r
source("install_packages.R")
```

or from a terminal:

``` bash
Rscript install_packages.R
```

## Example

### Cytation 7 example output file

-   The provided example shows the plate layout used for testing 6 phages (Bestiario, Cronopio, Roth32, Roth76, and Roth80) against 3 bacterial isolates (ARLG-3415, ARLG-4501, ARLG-4653).
-   Wells where phage and bacteria were mixed received bacteria at a final concentration of 1e+05 CFU/mL and phage at a final concentration of 1e+06 PFU/mL, and are denoted as 'Phage:isolate#'.
-   Bacterial controls were placed in column 10, rows A-F (final concentration 1e+05 CFU/mL).
-   Medium sterility controls were placed in row G, columns 1-3.
-   Phage controls were placed in row H, columns 1-7.
-   As bacterial cultures were adjusted to 1e+08 CFU/mL (OD600=0.18), extra wells in the plate were used to perform serial dilutions to get to the desired concentration for the assay. These wells are denoted 'isolate \# e7' or 'isolate# e6'. Users may decide to exclude the wells used for dilutions from their plate layout file for simplicity as only the bacterial controls are needed to compute hold times. In this example, they were included for transparency as absorbance data was collected during the run.

The provided Cytation 7 example output file matches the plate layout detailed above. Headings automatically created by the Cityation 7 software (displaying software version, assay date, and conditions) have been removed; ***this is a necessary step to run the code.***

Open `Growth_curves.Rproj` in RStudio, then run:

``` r
source("Example/Example_RunAnalysis.R")
```

Alternatively, from the project directory:

``` bash
Rscript Example/Example_RunAnalysis.R
```

The example uses:

``` text
Example/Example_data_1.txt
Example/plate_layout.xlsx
```

and writes the example results to the corresponding output folder.

## Analyzing your own data

Run:

``` r
source("RunAnalysis.R")
```

or from a terminal:

``` bash
Rscript RunAnalysis.R
```

The script will ask for two files:

``` text
Plate-reader .txt file path:
Plate-layout .xlsx file path:
```

Enter or paste the full path to each file when prompted.

Example:

``` text
Plate-reader .txt file path: /path/to/my_experiment.txt
Plate-layout .xlsx file path: /path/to/my_plate_layout.xlsx
```

## Input files

### Plate-reader data

The plate-reader file must be a tab-delimited `.txt` file containing:

-   a `Time` column
-   well columns identified as `A1` through `H12`

Only wells defined in the plate-layout file are included in the analysis.

### Plate layout

The plate layout must be an `.xlsx` Use the template located in the folder `Example` to upload your own layout taking into consideration:

-   Rows correspond to plate rows `A` through `H`.
-   Columns correspond to plate columns `1` through `12`.
-   Cells contain sample names.
-   Empty wells may be left blank.
-   Wells with the same sample name are treated as replicates.

## Analysis

For each populated well, the workflow:

1.  Converts elapsed measurement time to hours.
2.  Fits a smoothing spline to OD600 versus time using `spar = 0.6`.
3.  Evaluates the second derivative over 4,000 evenly spaced time points.
4.  Defines hold time from the maximum second derivative, beginning from the fourth derivative point.
5.  Assigns a hold time of 24 hours when the mean OD600 remains below the threshold of `0.150`.
6.  Groups replicate wells by sample name and calculates the mean inflection time, standard deviation, replicate count, and individual replicate values.

The analysis parameters are defined in `R/hold_time_analysis.R`.

## Output

The analysis generates:

-   an interactive `.html` report containing the plate layout, sample summary, and individual growth curves
-   a `.tsv` table containing the summarized inflection-time results

Generated output should not be treated as source code and normally does not need to be committed to GitHub.

## Recommended workflow

For the included example:

``` bash
Rscript Example/Example_RunAnalysis.R
```

For your own experiment:

``` bash
Rscript RunAnalysis.R
```

The main analysis code is kept separately in:

``` text
R/hold_time_analysis.R
```

This allows users to run the workflow without modifying the analysis functions or parameters.
